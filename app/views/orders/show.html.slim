container.col-xs-12.col-md-6.col-md-offset-3.container-white.space-up
  h3.text-center Order from #{@order.created_at.strftime("%d/%m %H:%M")}
  hr
  = render partial: 'order_item_row', collection: @order.order_items, as: :item