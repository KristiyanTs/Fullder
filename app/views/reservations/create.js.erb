<% if @reservation.errors.any? %>
  $('.container-white').html("<%= escape_javascript (render partial: 'form') %>");
  end
<% else %>
  $('.container-white').html("<%= escape_javascript (render partial: 'successful') %>");
<% end %>
